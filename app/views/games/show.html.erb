<div class="container pt-5" >
  <p><%= @game.neighborhood %></p>
  <p><%= l @game.date, format: :long %></p>
  <p><%= l @game.time, format: :default %></p>
  <%= link_to 'Ver todas as peladas', root_path %>
</div>
